# Hints

1. Open with write mode and use `write()`.
2. Iterate over the file object.
3. Read all text and split it into words.
4. Open using append mode.
5. Read the source, then write the destination.
6. Split each line and convert the target column.
7. Use the `json` module.
8. Load JSON into Python objects and filter them.
9. Decide the file format before writing features.
10. Store one record per line or use JSON consistently.
