# Hints

1. Remove spaces before counting, or count only non-space characters.
2. Slicing can step backwards.
3. Compare the string with its reverse.
4. Normalize case and check membership in `aeiou`.
5. Use `strip()` and `title()`.
6. Split on whitespace and join with one space.
7. `split()` gives words.
8. Use `find()` or `in`.
9. Normalize and compare sorted characters.
10. Combine length, `isalnum()`, and a username policy.
