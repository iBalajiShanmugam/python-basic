# Solution notes

1. `name = input("Name: "); print(f"Hello, {name}!")`.
2. `a = int(input()); b = int(input()); print(f"Sum: {a + b}")`.
3. `print(year, month, day, sep="-")`.
4. Convert quantity and price, then print `quantity * price`.
5. `salary = float(hours) * float(rate)`.
6. `sep` is placed between values; `end` replaces the final newline.
7. Read Celsius as float and apply the conversion formula.
8. Use an f-string with fixed widths.
9. Use a header, one line per field, and a final total.
10. State types, prompts, and formatting before writing code.
