# Solution notes

1. `def square(number): return number * number`.
2. `return number % 2 == 0`.
3. Start with `largest = first` and compare the other two.
4. Calculate subtotal, then subtract `subtotal * discount`.
5. `sum(1 for ch in text.lower() if ch in "aeiou")`.
6. Define `add`, `subtract`, `multiply`, and `divide`.
7. Return `[factor for factor in range(1, number + 1) if number % factor == 0]`.
8. Check length, digit, and letter rules in order.
9. `return 1 if n == 0 else n * factorial(n - 1)`.
10. Use small functions with explicit parameters and return values.
