# Solution notes

1. Use `if number > 0`, `elif number < 0`, otherwise zero.
2. `largest = a if a > b else b`.
3. Start with `largest = a`, then compare `b` and `c`.
4. `for number in range(1, 21)`.
5. Add an `if` inside the loop.
6. Initialize `total = 0` and add each number.
7. `leap = year % 400 == 0 or (year % 4 == 0 and year % 100 != 0)`.
8. Loop until the user chooses `q`.
9. `for multiplier in range(1, 11)`.
10. Reject a number when any divisor divides it evenly.
