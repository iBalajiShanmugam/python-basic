# Hints

1. Catch `ValueError` around `int()`.
2. Catch `ZeroDivisionError`.
3. Catch `FileNotFoundError`.
4. Check the value and use `raise ValueError(...)`.
5. Put input inside a loop and return only after success.
6. Read the exception type and the failing operation.
7. Put one operation attempt inside the loop.
8. Check membership in the allowed choices.
9. `finally` runs whether an exception occurs or not.
10. Validate each field before saving it.
