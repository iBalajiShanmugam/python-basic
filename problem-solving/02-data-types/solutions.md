# Solution notes

1. `name: str`, `age: int`, `salary: float`, `code: str`, `active: bool`, missing value: `None`.
2. Print each result from `type()`.
3. `area = length * width`.
4. `phone = "0123456789"`.
5. `free_delivery = total >= limit`.
6. Numbers remain numeric; quoted values are strings; comparisons return booleans.
7. A postal code and a quantity may both look numeric but support different operations.
8. Use one variable per field and an f-string.
9. `if value is None: ...`.
10. Record the field, chosen type, and reason in a table or dictionary.
