# Hints

1. Use `//` and `%`.
2. Formula: principal times rate times time divided by 100.
3. Combine two comparisons with `and`.
4. Combine permission conditions with `and` or `or`.
5. Apply discount before tax if that is the stated business rule.
6. Parentheses make precedence explicit.
7. Test with 17 and 5.
8. Use `in`.
9. Use `==` for values and `is None` for `None`.
10. Translate each business rule into a boolean expression.
