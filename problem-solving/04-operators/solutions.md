# Solution notes

1. `quotient, remainder = divmod(a, b)`.
2. `interest = principal * rate * years / 100`.
3. `lower <= value <= upper`.
4. Example: `is_allowed = logged_in and is_member`.
5. Calculate `discounted = price * (1 - discount)` then tax.
6. Multiplication and division happen before addition; use parentheses to verify.
7. `/` returns a true quotient, `//` floors, `%` returns the remainder.
8. `character in text`.
9. `left == right`.
10. Name intermediate booleans such as `within_weight_limit` for clarity.
