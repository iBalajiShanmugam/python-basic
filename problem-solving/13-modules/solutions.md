# Solution notes

1. Define `greet(name)` in `greetings.py` and import it.
2. Both import styles work; choose the one that keeps names clear.
3. `rates.py` should contain named constants such as `GST_RATE`.
4. `if __name__ == "__main__":` protects demonstration code during import.
5. Use `operations.py` and `main.py`.
6. Put prime, factorial, and divisor functions in `number_utils.py`.
7. `import number_utils as numbers`.
8. Create a package folder with focused modules.
9. Remove one import cycle by moving shared code down into a neutral module.
10. Use `storage.py`, `expense_service.py`, and `main.py`.
