# Solution notes

1. `sum_value = 0` and add each item.
2. Initialize `largest = numbers[0]` and compare the rest.
3. Maintain `even_count` and `odd_count`.
4. `result = []; if item not in result: result.append(item)`.
5. `reversed_list = values[::-1]`.
6. `rotated = values[-1:] + values[:-1]`.
7. `merged = sorted(left + right)`.
8. `unique = sorted(set(values)); unique[-2]` after checking length.
9. `year, month, day = date_tuple`.
10. Use one pass for total and comparisons, then calculate average.
